const baseServiceURL = "http://localhost:8080/api/v1/"

const urls = {
     projects: "/projects/",
     tasks: "/tasks/",
     users: "/users/",
}

exports.urls = urls
exports.baseServiceURL = baseServiceURL